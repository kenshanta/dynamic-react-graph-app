import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgb(0, 032, 091)",
    },
    secondary: {
      main: "rgb(186, 12, 46)",
    },
    info: {
      main: "rgb(0, 032, 091)",
    },
  },
});

export default theme;
